name: Build ORIGIN-CORe EXE

on:
  push:
    branches: [ "main" ]
    tags: [ "v*.*.*" ]
  workflow_dispatch: {}

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Ensure libs folder has a placeholder file
        run: |
          New-Item -ItemType Directory -Force -Path "libs" | Out-Null
          if (-not (Get-ChildItem -Path "libs" -Force)) {
            New-Item -ItemType File -Force -Path "libs\.gitkeep" | Out-Null
          }
          Write-Host "Contents of libs:"
          Get-ChildItem -Path "libs" -Force
        shell: pwsh

      - name: Build EXE with PyInstaller
        run: |
          pyinstaller --noconfirm --onefile --windowed --name ORIGIN-CORe --add-data "libs;libs" main.py
        shell: pwsh

      - name: Upload EXE as artifact
        uses: actions/upload-artifact@v4
        with:
          name: ORIGIN-CORe-windows
          path: dist/ORIGIN-CORe.exe

      - name: Create GitHub Release (on tag push)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/ORIGIN-CORe.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
